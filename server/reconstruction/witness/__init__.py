"""Witnesses of the metric cloud (claude_stac.txt §6): every point carries
who vouches for it — ``mv_votes`` (neighbouring keyframes that agree on the
surface, §6.1), ``mask_votes`` / ``mask_conflicts`` (SAM3 masks of the same /
another instance the point projects into, §6.2) and a ``status`` derived from
configurable rules (§6.3). Nothing here deletes a point: the fields are
evidence, the cleaning stage and the viewer act on them.
"""
