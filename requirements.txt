# ===========================================================================
# STAC-Builder — Unified Requirements
# Covers: stac-builder server, DA3 (vendor), SAM3 (vendor), scene_analyzer
# 
# Install:
#   pip install -r requirements.txt
#
# NOTE: PyTorch must be installed FIRST with the correct CUDA version.
#       See Dockerfile or run:
#         pip install torch==2.5.1 torchvision==0.20.1 torchaudio==2.5.1 \
#           --index-url https://download.pytorch.org/whl/cu121
# ===========================================================================

# ── Core ML ──────────────────────────────────────────────────────────
torch>=2.5.0
torchvision>=0.20.0
torchaudio>=2.5.0
xformers>=0.0.28
einops>=0.8.0
timm>=1.0.17
safetensors>=0.4.0

# ── DA3 dependencies ────────────────────────────────────────────────
huggingface_hub>=0.20.0
trimesh>=4.0
open3d>=0.18
evo>=1.34
e3nn>=0.5
omegaconf>=2.3
plyfile>=1.1
pillow_heif>=0.18
pycolmap>=3.10
moviepy==1.0.3
faiss-cpu>=1.7.0
numba>=0.59.0
pypose>=0.6.0

# ── SAM3 dependencies ───────────────────────────────────────────────
tqdm>=4.65
ftfy>=6.1
regex>=2023.0
iopath>=0.1.10
typing_extensions>=4.0

# ── Scene Analyzer (InternVL3) ───────────────────────────────────────
transformers>=4.40.0,<4.48.0
accelerate>=1.1.0
bitsandbytes>=0.43.0

# ── STAC server ──────────────────────────────────────────────────────
fastapi>=0.100.0
uvicorn>=0.30.0
websockets>=12.0
python-multipart>=0.0.6
pydantic>=2.0

# ── Image / Video processing ────────────────────────────────────────
opencv-python>=4.8
Pillow>=10.0
imageio>=2.30
decord>=0.6.0

# ── Scientific computing ────────────────────────────────────────────
numpy<2
scipy>=1.10

# ── Utilities ────────────────────────────────────────────────────────
requests>=2.31
typer>=0.9.0
PyYAML>=6.0
python-dotenv>=1.0
rich>=13.0
