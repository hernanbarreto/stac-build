"""Metrics harness of the metric cloud (claude_stac.txt §10) — evidence, never
a verdict. F2 ships the single-variable A/B of `elastic_seam` (§7);
F3 completes the harness (per-epoch report, known-answer and collapse
experiments, adversarial suite, determinism).
"""
